package server;

/**
 * Handles file transfer logic between clients through or mediated by the server.
 */
public class FileTransferService {

    public void initTransfer(String sender, String receiver, String fileName, long fileSize) {
        // Initialize file transfer metadata
    }

    public void forwardFileData(byte[] data, int bytesRead) {
        // Forward file chunk data
    }
}
